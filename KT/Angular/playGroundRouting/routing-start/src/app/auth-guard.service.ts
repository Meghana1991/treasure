import { CanActivate, ActivatedRoute, RouterStateSnapshot } from "@angular/router";
import { Observable } from "rxjs";

export class AuthGuard {
    // canActivate(route: ActivatedRoute, state: RouterStateSnapshot): Observable {
        
    // }
}