const EventEmitter = require('events'); //CLASS
//EventEmitter is a class hence as a good convention we need to make first letter upper case

const emitter = new EventEmitter(); //OBJECT

//A class is like human, an object is actual person ie manju soumya